#include <stdio.h>
#include <unistd.h>

void ft_print_reverse_alphabet  (void)
{
  int i=0;
  char c='z';
  while (i<26)
    {
      write(1, &c, 1);
      i++;
      c--;
    }
}

int main ()
{
  ft_print_alphabet();
  return 0;
}
