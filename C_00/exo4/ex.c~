#include <stdio.h>
#include <unistd.h>


void ft_is_negative (int num)
{
  char p='P';
  char n='N';
  if (num < 0)
    {
      write (1, &p, 1);
    }
  else
    {
      write(1, &n, 1);
    }
}

int main ()
{
  ft_is_negative(-42);
}
